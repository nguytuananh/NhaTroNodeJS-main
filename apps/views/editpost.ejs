<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa Bài Đăng</title>
    <link rel="stylesheet" href="/static/css/styles.css">
    <link rel="stylesheet" href="/static/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="container">
        <div class="sidebar" id="sidebar">
            <button class="toggle-btn" onclick="toggleSidebar()">☰</button>
            <ul>
                <li><a href="/admin/profile"><i class="fas fa-user"></i><span>Quản lý tài khoản</span></a></li>
                <% if (user.role === 'admin') { %>
                    <li><a href="/admin/dashboard"><i class="fas fa-chart-line"></i><span>Dashboard</span></a></li>
                    <li><a href="/admin/users"><i class="fas fa-users"></i><span>Quản lý người dùng</span></a></li>
                <% } %>
                <li><a href="/admin/posts"><i class="fas fa-file-alt"></i><span>Quản lý bài đăng</span></a></li>
                <li><a href="http://localhost:3000/"><i class="fas fa-home"></i><span>Trang chủ</span></a></li>
                <li><a href="/user/logout"><i class="fas fa-sign-out-alt"></i><span>Đăng xuất</span></a></li>
            </ul>
        </div>
        <div class="main-content" id="main-content">
            <h2>Sửa Bài Đăng</h2>
            <% if (error) { %>
                <p style="color: red;"><%= error %></p>
            <% } %>
            <form action="/admin/posts/edit/<%= post._id %>" method="POST">
                <div class="form-group">
                    <label for="title">Tiêu đề:</label>
                    <input type="text" id="title" name="title" value="<%= post.title %>" required>
                </div>
                <div class="form-group">
                    <label for="location">Khu vực:</label>
                    <input type="text" id="location" name="location" value="<%= post.location %>" required>
                </div>
                <div class="form-group">
                    <label for="price">Giá (triệu/tháng):</label>
                    <input type="number" id="price" name="price" value="<%= post.price %>" step="0.1" required>
                </div>
                <div class="form-group">
                    <label for="area">Diện tích (m²):</label>
                    <input type="number" id="area" name="area" value="<%= post.area %>" required>
                </div>
                <div class="form-group">
                    <label for="description">Mô tả:</label>
                    <textarea id="description" name="description" rows="4"><%= post.description %></textarea>
                </div>
                <button type="submit">Cập nhật</button>
                <a href="/admin/posts" style="margin-left: 10px;">Hủy</a>
            </form>
        </div>
    </div>
    <script src="/static/js/sidebar.js"></script>
</body>
</html>