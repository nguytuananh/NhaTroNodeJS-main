<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Nhập - Thuê Nhà Trọ</title>
    <link rel="stylesheet" href="/static/css/styles.css">
</head>

<body>
    <%- include('partical/menu') %>

        <div class="auth-page">
            <h2>Đăng Nhập</h2>
            <form id="login-form" action="/user/login" method="POST">
                <div class="form-group">
                    <label for="username">Tên đăng nhập</label>
                    <input type="text" id="username" name="username" placeholder="Nhập tên đăng nhập" required>
                </div>
                <div class="form-group">
                    <label for="password">Mật khẩu</label>
                    <input type="password" id="password" name="password" placeholder="Nhập mật khẩu" required>
                </div>
                <button type="submit">Đăng Nhập</button>
            </form>
            <p class="auth-link">Chưa có tài khoản? <a href="/user/register">Đăng ký ngay</a></p>
        </div>

        <div class="footer">
            <p>© 2025 Thuê Nhà Trọ. All rights reserved.</p>
        </div>

        <!-- <script>
            document.getElementById('login-form').addEventListener('submit', async (e) => {
                e.preventDefault();
                const formData = new FormData(e.target);
                const response = await fetch('/user/login', {
                    методом: 'POST',
                    body: formData
                });
                const result = await response.json();
                if (result.status) {
                    localStorage.setItem('token', result.token); // Lưu token vào localStorage
                    window.location.href = '/post'; // Chuyển hướng sau khi đăng nhập
                } else {
                    alert('Đăng nhập thất bại!');
                }
            });
        </script> -->
</body>
</html>